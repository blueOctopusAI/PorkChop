import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "PorkChop — AI Bill Analyzer",
    template: "%s — PorkChop",
  },
  description: "AI that reads the bills so you don't have to. Spending breakdowns, pork scores, and plain English summaries for congressional legislation.",
  icons: {
    icon: "/porkchop-logo.jpg",
    apple: "/porkchop-logo.jpg",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
